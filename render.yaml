
services:
  - type: web
    name: epitools-streamlit-app
    env: python
    plan: free
    
    buildCommand: pip install -r requirements.txt
    
    startCommand: sh setup.sh && streamlit run app.py
    
    apt:
      - unixodbc
      - libgdal-dev
      - libgeos-dev
      - libproj-dev
